---
description: PostgreSQL 数据库规范（建表、索引、SQL、事务、连接池）
globs: "**/*.go, **/migrations/**, **/db/**, **/sql/**"
alwaysApply: false
---

# PostgreSQL 数据库规范

## 连接池配置

- 推荐公式：`MaxOpenConns = (核心数 × 2) + 有效磁盘数`
- 配置 `MaxIdleConns`、`MaxOpenConns`、`ConnMaxLifeTime`，禁止无限制开放连接
- 慢查询和长事务是连接池耗尽的主要原因，需及时处理

## 建表规范

- 【强制】每张表必须有主键
- 【强制】字符编码必须为 UTF8
- 【强制】作为查询条件（WHERE）或排序（ORDER BY）的字段必须建索引
- 【强制】唯一列或组合唯一列必须添加数据库层面的唯一约束（不得只靠应用层保证）
- 【强制】语义上无零值/空值区分的字段必须添加 NOT NULL 约束
- 【强制】有默认值的列必须添加 DEFAULT 子句

```sql
-- ✅ Good
CREATE TABLE user_activity (
    id BIGSERIAL PRIMARY KEY,
    uid VARCHAR(50) NOT NULL,
    create_time TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(uid, done_date)
);
```

- 【推荐】对象命名（表名、列名等）只用小写字母、下划线、数字，使用 snake_case，长度 ≤ 63 字符，禁止使用中文、保留字、`pg_` 前缀
- 【推荐】主键优先使用 `BIGSERIAL` 或 `IDENTITY` 列；有分片需求使用 UUID v7（禁止无序 UUID v4）
- 【推荐】尽量选择合适数据类型，能用数字不用字符串；字符串有长度限制用 `varchar(n)`，否则用 `text`；避免 `char(n)`
- 【推荐】稳定且取值空间小的字段使用枚举类型
- 【推荐】为对象添加注释（COMMENT），对象变更时同步更新注释

## 索引规范

- 【推荐】线上业务新建索引使用 `CREATE INDEX CONCURRENTLY`，避免锁表
- 【强制】禁止在 archery SQL 平台修改或删除索引，必须给运维提工单，使用 `CONCURRENTLY` 方式夜间执行

```sql
-- ✅ 在线创建索引
CREATE INDEX CONCURRENTLY ON user_active_record (uid);

-- ✅ 在线删除索引
DROP INDEX CONCURRENTLY idx_traffic_ua;
```

- 【推荐】禁止在大字段（text、超长 varchar）上建索引；被索引字段原则上不超过 64 字符
- 【推荐】高选择性列（唯一值多）才适合建索引；避免在频繁更新的列上建索引

## SQL 规范

- 【强制】事务内禁止长时间等待，开启后尽快提交/回滚，优先使用 ORM 框架事务管理

```go
// ✅ Good — 使用 gorm 事务管理
db.Transaction(func(tx *gorm.DB) error {
    if err := tx.Create(&item).Error; err != nil {
        return err  // 自动回滚
    }
    return nil  // 自动提交
})
```

- 【推荐】需要插入/修改后立即获取结果时使用 `RETURNING` 子句，减少数据库交互次数
- 【推荐】可空列有排序需求时，查询和索引中明确指定 `NULLS FIRST` 或 `NULLS LAST`
- 【避免】禁止在线业务全表扫描（小常量表、低频后台操作除外）
- 【避免】首层过滤条件禁止使用 `!=`、`<>`、NULL 判断（会导致全表扫描）
- 【避免】禁止使用数据库触发器实现业务逻辑
- 【避免】避免在数据库中执行复杂计算，迁移至应用层
- 【避免】游标（rows）使用后必须及时 `defer rows.Close()`

```go
// ✅ Good
rows, err := db.Model(&dao).Where(...).Rows()
if err != nil { return err }
defer rows.Close()
```
