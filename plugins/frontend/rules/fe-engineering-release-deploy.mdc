---
description: 多环境构建、产物与发布/部署检查项的通用规范。编辑 CI/CD、Dockerfile 或构建模式相关配置时加载。
globs:
  - "**/.github/workflows/**"
  - "**/.gitlab-ci.yml"
  - "**/Dockerfile*"
  - "**/vite.config.*"
  - "**/.env.example"
alwaysApply: false
---

# 前端工程：发布与部署

## MUST

- **环境显式化**：`development` / `staging` / `production`（或团队等价命名）的差异通过**构建模式 / 环境变量**注入，禁止在源码中写死「只有某台机器知道的 URL」。
- **配置与密钥分离**：部署所需的令牌、数据库连接等**不**进入前端仓库明文；由部署平台或运行时注入。
- **可回滚**：发布流程应支持**回滚到上一已知良好版本**（制品复用或重新构建可追溯 tag/commit）；具体实现依托管平台（容器镜像 tag、静态资源版本目录等）。
- **制品可追溯**：部署产物应能关联到 **Git commit**（或标签）；在界面或响应头中暴露版本信息为可选增强，由团队决定。

## SHOULD

- **预览环境**：为合并请求或主干提供**预览部署**时，使用隔离配置与域名，避免误连生产数据。
- **静态资源缓存**：长缓存与文件名指纹（content hash）配合使用；`index.html` 等入口采用较短缓存或不缓存策略，遵循静态托管最佳实践。

## MUST NOT

- 在生产构建中开启**开发专用**插件（如未脱敏的详细错误栈、仅 dev 可用的 mock 拦截）除非经安全评估。

## 对 AI / Agent

- 修改 `base`、`publicPath`、环境变量前缀或与 CDN/OSS 相关插件时，提醒核对**目标环境**与**非生产不得指向生产后端**。
- Monorepo 中明确「当前变更影响哪一个 app 包」，避免误改其他应用的部署配置。
