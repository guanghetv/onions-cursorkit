---
description: 构建、类型检查、测试与代码质量闸门的通用规范。编辑构建配置、测试配置或 CI 时加载。
globs:
  - "**/vite.config.*"
  - "**/vitest.config.*"
  - "**/playwright.config.*"
  - "**/cypress.config.*"
  - "**/eslint.config.*"
  - "**/.eslintrc*"
  - "**/tsconfig*.json"
  - "**/.github/workflows/**"
  - "**/.gitlab-ci.yml"
alwaysApply: false
---

# 前端工程：构建、测试与质量

## MUST

- **类型与构建**：在合并请求阶段，至少对改动部分执行**类型检查**（如 `vue-tsc` / `tsc`）与**可重复的构建**；具体命令以项目脚本为准，须写入文档或 CI。
- **Lint 与格式**：团队须启用 **ESLint**（或等价）并与构建工具生态兼容；**格式化**（Prettier / dprint 等）策略二选一或组合时须全仓统一，避免同仓库多风格。
- **CI 为最终裁判**：本地可通过 hook 辅助，但**不得以「我本地没跑」为由**跳过 CI；CI 失败禁止合并（除非团队明文规定的例外流程）。
- **测试分层**：至少具备一类**快速反馈**的自动化测试（如单元/组件测试）；关键路径在条件允许时补充 **E2E**；新功能应对应可维护的测试或书面豁免理由。

## SHOULD

- **共享配置基座**：ESLint、TypeScript 配置优先**继承**仓库内已存在的团队共享基座（例如已发布的 `@scope/eslint-config`、根目录 `tsconfig` / `tsconfig.base` 并由子包 `extends`）；新建应用或子包时**复用**既有基座，避免复制一份独立配置导致规则漂移。
- **产物与 source map**：生产环境 source map 策略遵循安全与排障平衡（如内网可见、不对外暴露）；不默认向公网暴露完整源码 map。
- **Bundle 分析**：周期性使用官方或社区推荐的 **bundle 分析工具**检查体积回归，尤其对大型依赖与动态 import。
- **浏览器目标**：在 `browserslist` 或构建插件中明确目标环境，与产品支持的客户端范围一致。

## MUST NOT

- 通过关闭类型检查、忽略全部 ESLint 规则等方式「强行绿灯」合入，除非极小范围且有跟踪项修复。

## 对 AI / Agent

- 修改构建配置后，应建议执行项目已有的 **`build` / `lint` / `test`** 脚本之一验证，**不**编造不存在的脚本名。
- Monorepo 下使用编排工具的 **filter** 仅对受影响包运行检查，符合团队 CI 习惯即可。
