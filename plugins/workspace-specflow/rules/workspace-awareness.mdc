---
description: 当前工作区是一个 specs 仓库（含 requirements/ 和 workspace-repos.json），启用 workspace-specflow 多角色协作工作流。
globs:
  - "requirements/**"
  - "**/workspace-repos.json"
---

# Workspace Specflow 多角色协作工作流（全局意识）

## 识别

当前项目含有 `requirements/` 目录和 `workspace-repos.json`，是一个 **specs 仓库**（需求层）。

## 分层说明

- **需求仓库（当前）**：需求管理、prd.md（飞书七章 + 5稿/9稿）、测试 spec、进度追踪。依赖 Superpowers。
- **代码仓库（其他）**：技术设计、TDD 开发、验证、归档。

当前仓库始终作为需求真相源，代码仓库按需求语义和 MODULE 边界理解并执行。

## 可用技能

| 命令 | 角色 | 用途 |
|------|------|------|
| `/req-new` | 任何人 | 初始化需求目录（中文目录名 + 英文 id slug；飞书七章骨架） |
| `/pm-proto` | 产品 | 原型快速生成或迭代（可选） |
| `/pm-spec-5` | 产品 | 5稿结构化增强（内审/交互评审前；允许待定项） |
| `/pm-spec` | 产品 | **9稿定稿**（交互评审后；严格 AI Review；`prd.status=confirmed`） |
| `/prd-feishu-sync` | 产品 | PRD ↔ 飞书同步（create/push/reconcile；禁止默认整篇覆盖） |
| `/prd-consistency-check` | 产品 | 契约层一致性校验（结构 A + 语义 B）；结果回写飞书 |
| `/prd-publish` | 产品 | 一键：sync → consistency-check |
| `/qa-spec` | 测试 | prd.md → 测试 spec + 可选 XMind 导出（禁止读取开发产出） |
| `/qa-sync-xmind` | 测试 | test-spec.md ↔ XMind |
| `/qa-execute` | 测试 | test/*.md → Case Flow 快速模式上传 |
| `/req-status` | 任何人 | 查看 PRD 5稿/9稿 与测试 spec 状态 |

## 需求目录命名

- **目录名**：清洗后中文（如 `requirements/订单退款流程优化/`），便于文件管理器查找
- **metadata.id**：英文关键词 kebab-case slug（如 `order-refund-flow-opt`），创建后不变，供 openspec/脚本引用
- **重名消歧**：`-2`、`-3`… 或 `-MMDD`；不用随机数
- **既有英文目录**（如 `contract-subject-tree/`）保留，与新中文目录共存

## 前置：确保 specs 仓库最新

读取 `requirements/` 前提示 `git pull`。behind remote 时**必须先提示 pull**。

## 产品流程脑暴门禁（`/pm-proto`、`/pm-spec-5`、`/pm-spec`）

写入 `prototypes/` 或结构化改写 `prd.md` **之前**，必须：

1. Read 并遵循 `superpowers:brainstorming`
2. 与用户完成灰区确认（一次一问）
3. 用户明确放行

## 关键约束

- 需求层文件只在 `requirements/` 下操作
- `/qa-spec` 禁止读取 `openspec/changes/`
- prd.md 对齐飞书模板（https://guanghe.feishu.cn/docx/S38Id4fxAofdz8xsWCVcRkHjnHg）；契约补充「关键关注」「回归」（语义定位，序号仅兼容）
- **开发禁止修改 requirements/ 下的文件**
- 5稿快照：`snapshots/prd-v5-*.md`；9稿快照：`snapshots/prd-v9-*.md`
- 版本表（第二章）仅在 `/pm-spec-5` 或 `/pm-spec` **确认时**追加行
- 开发在代码仓库按 fe-specflow / be-specflow / onion-sdd 启动；直接读取已 `confirmed` 的 `prd.md`，不再提供 `/dev-start`
- 飞书同步禁止默认整篇 overwrite；9 稿按语义删背景/价值且不重排关键关注/回归与四～七；`v9_synced` 后默认不再推 5 稿

## 流程阶段

1. **需求发起**：`/req-new` → 中文目录 + 飞书七章骨架 + **`/prd-feishu-sync create`**
2. **原型（可选）**：`/pm-proto`
3. **5稿**：`/pm-spec-5` → 内审；未 `v9_synced` 时 sync push v5
4. **交互评审**：产品可手工改 `prd.md` / 飞书；契约变更须 sync 或 reconcile
5. **9稿**：`/pm-spec` 瘦身 → push v9 → **consistency-check** → `prd.status=confirmed`
6. **一键发布（可选）**：`/prd-publish`
7. **测试 spec**：`/qa-spec`（与开发并行；启动前读 consistency）
8. **Case Flow 执行（可选）**：`/qa-execute`
9. **开发**：代码仓库流程读取 `prd.md`（要求 `prd.status = confirmed`）
10. **进度追踪**：`/req-status`

## 需求定位

- 扫描 `requirements/` 下子目录的 `metadata.yaml`
- 列表显示：`name（id: slug）` 或中文目录名
- `/pm-spec-5` 过滤 `prd.stage` 为 v5_pending / v5_confirmed 且 9稿未完成的需求
- `/pm-spec` 过滤待 9稿定稿的需求
- `/qa-spec` 与代码仓库开发流程要求 `prd.status = confirmed`

## 权限模型

| 角色 | requirements/<req>/ 读 | requirements/<req>/ 写 | 代码仓库 |
|------|----------------------|----------------------|---------|
| 产品 | prd.md, metadata.yaml | prd.md, prototypes/, metadata.yaml, snapshots/（确认时） | 只读 |
| 测试 | prd.md | test/test-spec.md, metadata.yaml | 只读 |
| 开发 | prd.md, test/test-spec.md | 不修改 `requirements/` 下文件 | 读写 |
