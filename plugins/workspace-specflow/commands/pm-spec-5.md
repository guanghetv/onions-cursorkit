---
name: pm-spec-5
description: 5稿 PRD 结构化增强：内审与交互评审会前；允许待定项；轻量 AI Review；确认后快照至 snapshots/prd-v5-*.md。
---

# /pm-spec-5

5稿结构化增强 `prd.md`，用于产品内审与交互评审会。

**门禁**：须先 **Read 并遵循 `superpowers:brainstorming`**，确认 MODULE 初拆与灰区后，才可改写 `prd.md`。

允许 `[待定]` / `[待交互确认]`。确认后快照 `snapshots/prd-v5-<date>.md`，更新 `prd.v5.status`；**不** 触发 `prd.status = confirmed`。

交互评审后执行 `/pm-spec`（9稿定稿）。
