---
description: Onion SDD guardrails for mini/light/full OpenSpec changes.
globs:
  - "openspec/**"
  - "**/*.vue"
  - "**/*.tsx"
  - "**/*.jsx"
  - "**/*.ts"
  - "**/*.js"
  - "**/*.css"
  - "**/*.scss"
  - "**/*.less"
---

# Onion SDD 规则

当编辑 `openspec/**` 或处理 Onion SDD slash command 时，遵循本规则。

## 触发边界

- 当前承诺 slash command 触发：`/onsf-fix`、`/onsf-tweak`、`/onsf-plan`、`/onsf-continue`、`/onsf-finish`、`/onsf-auto`。
- `/onsf-auto` 是无交互自动化入口，使用 `skills/auto-flow/SKILL.md` 编排现有 Onion SDD skills；metrics、Spec Pack 等能力仍可按后续验收或独立任务推进。
- OpenSpec 是变更正文唯一真相源；运行态通过 `scripts/onion_state.py` 统一读写。
- **读优先级**：已绑定 Trellis `meta.onion` → `.onion-sdd/current.json` → OpenSpec 扫描。
- **写优先级**：已绑定 Trellis task 时主写 `meta.onion` 并镜像 `current.json`；无 Trellis / 未绑定时只写 `current.json`。
- **repo-root 解析**：未显式传 `--repo-root` 且未设 `ONION_SDD_ROOT` 时，`onion_state.py` 从 cwd 向上查找最近的含 `.trellis/` 的目录作为 repo-root，找不到回退 cwd。优先级：显式 `--repo-root` > `ONION_SDD_ROOT` > 自动解析。monorepo 子包 cwd 会自动定位到外层根；手动调用优先让脚本自动解析，避免硬编码 `--repo-root .`。
- 阶段切换、0++ 标记/清 pending、finish 归档成功后置 idle：**必须**调用 `onion_state.py`（无 Hook；靠本规则与 command 硬纪律）。
- `/onsf-finish` 归档前**必须**先跑 `scripts/finish_check.py`；exit ≠ 0 时禁止 `openspec archive` 与手工移动归档。

## 分级写入

- Tier 0：不创建 OpenSpec。
- Tier 0+：使用 `skills/mini-change/SKILL.md`。
- Tier 1：使用 `skills/light-change/SKILL.md`。
- Tier 2+：使用 `skills/full-change/SKILL.md`、`skills/openspec-change/SKILL.md`、`skills/external-spec/SKILL.md`、`skills/pull-yapi/SKILL.md`、`skills/re-check/SKILL.md`、`skills/verify-change/SKILL.md`。
- 自动化流程：使用 `skills/auto-flow/SKILL.md`，再按 Tier 路由到对应 skill。
- 需要跨会话恢复或同步 Trellis task metadata 时，使用 `skills/trellis-adapter/SKILL.md`。
- 飞书项目卡片进入实现前如需创建开发分支，可调用 Common 插件的 `create-feature-branch` 扩展能力；该能力不是 onion-sdd 硬依赖，缺失时提示用户同步 Common skill 或手动建分支。
- 创建 feature branch 成功且存在 Trellis task 时，用 Trellis 标准 `branch` 字段记录分支；不要把分支名重复写入 `meta.onion`。

## 写入门禁

写入 `openspec/changes/**` 前必须满足：

- 已完成 Tier 判断。
- Tier 0+/1 已按 mini/light 模板确认目标、范围和验证。
- Tier 2+ 已完成需求澄清、范围边界、验收口径和用户落盘确认。
- `/onsf-auto` 无交互模式下，以 `auto-flow` 的 auto 判断、风险门禁、继续假设和 spec 自审替代中途用户确认；高风险或无法补齐的关键需求仍必须停止。

修改业务代码前应已存在当前 change 的 `tasks.md`；纯文档、配置或紧急 Tier 0++ 可按对应轻量规则处理并补记录。

`/onsf-auto` 可在低/中风险缺口下记录假设并继续；遇到权限、安全、支付、资金、审计、破坏性数据行为、接口删除/重命名、QA/YApi 冲突、关键路径不可验证、需要创建/启动/归档 Trellis task、需要 commit/push/PR 等高风险或不可逆动作时必须停止。`/onsf-finish` 必须先跑 `finish_check.py`；通过后自动执行 `openspec archive <change-id>`，失败时停止。Tier 0++ `tier0pp_openspec_pending` 逾期且无 `## 带债项` 时预检 hard fail。

### 分支门禁

修改业务代码前（即将进入 implement 阶段，与上方"修改业务代码前应已存在 `tasks.md`"是同一时间点），必须确认满足以下两条，否则触发门禁。OpenSpec `proposal.md`/`tasks.md` 草稿阶段不受此门禁约束——未提交的草稿文件会随 `git checkout -b` 一起带入新分支，不需要先建分支再写文档。

**触发条件 1：当前分支是受保护分支**（固定列表，不做可配置项）：
- 精确匹配：`master`、`main`、`develop`
- 前缀匹配：`release/*`
- `git branch --show-current` 输出为空（detached HEAD）按受保护分支同等处理

**触发条件 2：当前分支绑定着另一个活跃 change**（检测逻辑见 `tier-triage/SKILL.md`「冲突检测」，本节只定义命中后的动作）：
- 双层判定，优先级从高到低：① 存在 Trellis active task 且 `task.json.branch` 等于当前分支，取该 task 的 `meta.onion.change_id`；② 无 Trellis/未绑定时，解析当前分支名是否匹配 `feat/<change-id>` 格式且对应 `openspec/changes/` 下真实存在的未归档目录，取解析出的 change-id。
- 判定出的 change-id 不等于本次要处理的 change-id 时触发。
- 两层都无法判定归属（无 Trellis 绑定，且分支名不匹配 `feat/<change-id>` 或找不到对应目录）时不触发——避免信息不足导致误判。

**命中任一触发条件时**：
1. 停止修改业务代码，向用户说明当前处于受保护分支 / 当前分支已绑定另一个 change `<change-id-A>`（触发条件 2 时，必须点名 `<change-id-A>`，并说明"继续会把两次不相关改动混进同一分支的 commit 历史"，不能用受保护分支场景的通用文案带过）。
2. 用户提供了飞书项目卡片链接 → 按 `full-change/SKILL.md`「开发分支准备」调用 `create-feature-branch` 创建分支。
3. 用户没有飞书链接，或本次是 Tier 0++ 紧急修复 → 提供分支名 `feat/<change-id>`（`change-id` 为当前 OpenSpec change 的 `MM-DD-<slug>`）供用户确认后创建，或允许用户自行创建分支后告知分支名。
4. 用户明确要求"就在当前分支继续改" → 尊重用户选择，记录在最终输出或 `proposal.md` 中，本次 change 生命周期内不再重复拦截。

**`/onsf-auto`（无交互自动模式）特化**：不拦截、不停止。命中任一触发条件时，直接按模板 `feat/<change-id>` 生成分支名并 `git checkout -b` 切换，无需确认；在最终输出中说明已自动创建并切换的分支名。触发条件 2 命中时，最终输出必须在风险/blocker 清单里显式点名"检测到跨 change 分支复用，已自动切换到新分支 `feat/<change-id>`，原分支 `<原分支名>` 绑定的 `<change-id-A>` 未受影响"，不能和触发条件 1 的常规提示合并成一句话。若已有飞书链接上下文但 auto 模式无法交互确认，同样直接走 `feat/<change-id>` 模板，不额外尝试调用 `create-feature-branch`（该 skill 依赖飞书 MCP 查询迭代信息，在无交互场景下不适合自动触发）。

Tier 0（纯问答/排查/审阅，无写入动作）不受本门禁约束。

## 实现纪律

修改业务代码时应遵守以下 TDD 与验证纪律：

- 能写自动化测试的任务遵守 TDD 红绿循环：失败用例 → 最小实现 → 通过；不得先实现再补测试。
- 前端任务优先分层验证：L1 契约/mock、L2 行为 Scenario、L3 联调/真实 API、L4 Browser 交叉验证。
- 无测试工具或任务性质不适合 TDD（纯配置、文档、紧急 Tier 0++）时，必须在 `tasks.md` 或验证报告中明确记录静态检查、手动验证或浏览器验证步骤，不得虚构已跑测试。
- 进入 verify 阶段前，先在 `verify-change` 中给出 TDD/静态验证清单结论，再考虑浏览器自动化。
- 发现升级红线或范围膨胀时，暂停并回到 triage/design。

## 代码审查

审查有两个触发点：check 阶段的主审查，以及提交时的条件化复审。审查基线统一为**暂存区**，与 `aicr-local` 既有 `staged` 模式一致，`/cr` 原样调用，不覆盖它的任何步骤。

### check 阶段四步

实现完成后进入 check，由 Agent **自动串联**执行，用户无需输入任何命令：

1. `trellis-check`：lint、typecheck、测试、`.trellis/spec/` 对齐与 spec 回写，含其自身发现问题的修复。
2. 暂存本次 change 范围内的改动。
3. `/cr` 审查暂存区。
4. 发现问题则修复 → 回跑受影响的门禁 → 重新暂存 → 复审，循环至通过。

**顺序不可调换**：`trellis-check` 会修改代码（修 lint、补测试）。若先暂存再跑它，它的修复只落在工作区而不在暂存区，`/cr` 审到的就是未修复版本，最终提交物与审查对象脱节。

**第 4 步的通过判据**：CR 报告中属于本次 change 的 🔴 清零即为通过。🟠 由 Agent 逐条判断，修或不修都要在 check 输出中说明理由，但**不作为循环条件**——否则 CR 总能再提一条建议，循环没有终点。

**修复循环必须回跑门禁**：第 4 步的修复若触及逻辑（如抽取共享常量、补充边界判断），须重跑受该修复影响的 lint / typecheck / 测试，再重新暂存复审；纯格式或命名调整可直接重新暂存复审。按修复影响面选择回跑范围，**不要求**每次全量重跑 `trellis-check`。

暂存后暂存区为空（本次 change 无实际改动）时，输出提示并跳过 `/cr`，不报错、不阻塞 check。

### 暂存范围

- 只暂存属于本次 change 的改动，依据是本次实现实际触及的文件与 `tasks.md` 对应的产物；**禁止** `git add -A`。
- 归属无法判断的文件，列出清单请用户确认，不默认纳入。
- 暂存区已含本次 change 之外的内容时提示用户，但**禁止** `git reset` 或任何移除用户已暂存内容的动作——只增不减。CR 审到这部分内容的问题时只列出并标注归属，不计入第 4 步的通过判据。
- `/onsf-finish` 归档时的 `git add openspec/changes/` 是范围受限的 scoped chore（纯文件移动），不受本节暂存纪律约束。

### 授权边界

- check 阶段允许自动执行 `git add`（限上述暂存范围）与 `/cr`：前者可用 `git reset` 撤销，后者只读。
- check 阶段仍**禁止**自动执行 `git commit`、push、创建 PR/MR；这些动作必须用户明确授权。本次放宽的只有暂存，不含提交。
- `/onsf-auto` 同样适用：暂存与 `/cr` 可自动执行、不停止；遇到 `git commit`、push、PR/MR 仍按既有高风险清单停止。

### 提交门禁（条件化复审）

用户明确授权提交后，判断暂存区自 CR 通过后是否发生变化：

- 未变化 → 直接 `git commit`，不重复审查。
- 发生任何变化（**含新增暂存文件**）→ 重新 `/cr` 通过后再 commit。
- 无法判定（如跨会话上下文丢失）→ 按重审处理。

判定由 Agent 依据会话内上下文完成；**不引入**审查指纹机制，**不为此修改** `onion_state.py`。判定必须覆盖「新增暂存文件」，不能只看代码有没有改——CR 通过后新 `git add` 进来的文件从未被审过，是唯一的漏审入口。

### 职责切分

- `trellis-check`：可执行门禁（lint、typecheck、测试）、`.trellis/spec/` 对齐、spec 回写。
- `/cr`（`aicr-local`）：团队前后端规范、安全风险检测、影响范围分析、`openspec/specs/` 业务需求对齐。
- 二者不互相替代，也都不替代 `verify-change` 的验收与 E2E 门禁。
- 派发 `trellis-check` 时可声明人工 review 维度由后续 `/cr` 覆盖，但该切分是**弱约束**：`trellis-check` 仍按自身 `SKILL.md` 执行，结论重叠时合并去重即可，不得为强制切分而修改 Trellis 源码。

### 降级

- `/cr` slash command 不可用 → 读取并按 `aicr-local` 的 `SKILL.md` 审查暂存区。
- `aicr-local` 未安装或无法使用 → Agent 对暂存区 diff 自审，并注明团队规范维度未覆盖。
- 两条降级均**不阻塞** check；`aicr-local` 不是 onion-sdd 的执行依赖。

## 前端专项纪律

当修改前端业务代码且当前变更属于 Onion SDD Tier 2+ 时，补充以下纪律：

- 有 Figma、设计稿或截图时，只读取并实现本次范围相关规格；局部改版不得擅自扩大到整页。
- 进入前端实现前，应记录与本次相关的灰区决策：空态、加载态、错误态、防重复、分页、权限、响应式或动效。无关维度可跳过。
- 外部 spec 到达时优先使用用户提供的工作区文件；具备 `workspace-repos.json` 与 `requirement_ref` 时可走 workspace-native；失败后降级到远程链接或用户粘贴。
- 涉及接口变更且飞书卡片/需求文档中有 YApi 链接或 interfaceID 时，使用 `pull-yapi` 只读提取契约；T1 后 YApi 到达或用户说 re-check 时，使用 `re-check` 对齐 mock、类型、API 层和测试。
- 不默认搜索 YApi；只有用户明确要求搜索接口，或文档仅给接口名称且用户同意搜索时，才走 YApi 搜索并让用户确认候选。
- YApi 请求/响应字段、类型、必填、method/path 是接口契约最高依据；QA spec 是 E2E 验收最高依据；冲突必须记录并请用户或协作者裁决。
- 飞书卡片和需求文档可以作为需求来源；创建 feature 分支前必须先确认需求范围和当前 change 产物，且遵守 `create-feature-branch` 的工作区干净、基线分支、MCP 和 git 权限门禁。
- Browser 验证前必须先给出清单摘要并获得用户确认；`/onsf-auto` 中若无法取得确认，默认记录 blocker 或替代验证，不调用浏览器自动化，除非用户输入已明确要求自动浏览器验证且环境可用。默认使用产品内置浏览器能力，不默认改用用户自配 DevTools MCP。
- 以上补充不要求新增 brainstorming 硬门禁。

## 上下文纪律

- 按需读取与当前变更相关的需求、OpenSpec、代码、测试和验证材料。
- 发现升级红线时停止轻量流程，转入 `/onsf-plan`。
- 不修改试点目录外的既有插件，不把其他插件作为执行依赖。
- 不修改 Trellis 源码、`.trellis/scripts/**` 或 `.trellis/.runtime/**`；如必须改 Trellis 才能继续，先向用户确认。
- 不复制 OpenSpec 正文到 Trellis task PRD、task metadata 或 journal。
- OpenSpec `tasks.md` 记录产品/验收交付物；Trellis `implement.md` 记录工程执行计划、验证命令和回滚点。

## 收束纪律

- `/onsf-finish` 必须先跑 `finish_check.py`；预检失败不得 archive。门禁通过或用户明确同意带债归档时，自动执行 `openspec archive <change-id>`；CLI 不可用时使用等效手工归档；失败时停止并报告。
- 归档成功后必须 `onion_state.py set --idle`。
- 不自动提交 git commit。
- 输出完成结论时必须说明验证结果、未覆盖风险和是否建议归档。
- Tier 2+ 以 `e2e-report.md` 的 `## 验收结论` 或用户确认的等价验收证据作为归档判断入口。
