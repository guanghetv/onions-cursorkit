---
description: Onion SDD guardrails for mini/light/full OpenSpec changes.
globs:
  - "openspec/**"
  - "**/*.vue"
  - "**/*.tsx"
  - "**/*.jsx"
  - "**/*.ts"
  - "**/*.js"
  - "**/*.css"
  - "**/*.scss"
  - "**/*.less"
---

# Onion SDD 规则

当编辑 `openspec/**` 或处理 Onion SDD slash command 时，遵循本规则。

## 触发边界

- 当前承诺 slash command 触发：`/onion-hotfix`、`/onion-tweak`、`/onion-plan`、`/onion-continue`、`/onion-finish`。
- `/onion-auto` 不属于当前基座能力；metrics、marketplace、Spec Pack 等能力可按后续验收或独立任务推进。
- OpenSpec 是变更正文唯一真相源；Trellis adapter 只保存状态、引用、hash、parent/child 关系和 journal 摘要。

## 分级写入

- Tier 0：不创建 OpenSpec。
- Tier 0+：使用 `skills/mini-change/SKILL.md`。
- Tier 1：使用 `skills/light-change/SKILL.md`。
- Tier 2+：使用 `skills/full-change/SKILL.md`、`skills/openspec-change/SKILL.md`、`skills/external-spec/SKILL.md`、`skills/verify-change/SKILL.md`。
- 需要跨会话恢复或同步 Trellis task metadata 时，使用 `skills/trellis-adapter/SKILL.md`。

## 写入门禁

写入 `openspec/changes/**` 前必须满足：

- 已完成 Tier 判断。
- Tier 0+/1 已按 mini/light 模板确认目标、范围和验证。
- Tier 2+ 已完成需求澄清、范围边界、验收口径和用户落盘确认。

修改业务代码前应已存在当前 change 的 `tasks.md`；纯文档、配置或紧急 Tier 0++ 可按对应轻量规则处理并补记录。

## 前端专项纪律

当修改前端业务代码且当前变更属于 Onion SDD Tier 2+ 时，补充以下纪律：

- 有 Figma、设计稿或截图时，只读取并实现本次范围相关规格；局部改版不得擅自扩大到整页。
- 进入前端实现前，应记录与本次相关的灰区决策：空态、加载态、错误态、防重复、分页、权限、响应式或动效。无关维度可跳过。
- 外部 spec 到达时优先使用用户提供的工作区文件；具备 `workspace-repos.json` 与 `requirement_ref` 时可走 workspace-native；失败后降级到远程链接或用户粘贴。
- Browser 验证前必须先给出清单摘要并获得用户确认；默认使用产品内置浏览器能力，不默认改用用户自配 DevTools MCP。
- 用户要求提交时，提交前必须完成审查；有团队本地审查命令或 skill 时优先使用，否则 Agent 自审待提交 diff。
- 以上补充不要求新增 brainstorming 硬门禁。

## 上下文纪律

- 按需读取与当前变更相关的需求、OpenSpec、代码、测试和验证材料。
- 发现升级红线时停止轻量流程，转入 `/onion-plan`。
- 不修改试点目录外的既有插件，不把其他插件作为执行依赖。
- 不修改 Trellis 源码、`.trellis/scripts/**` 或 `.trellis/.runtime/**`；如必须改 Trellis 才能继续，先向用户确认。
- 不复制 OpenSpec 正文到 Trellis task PRD、task metadata 或 journal。

## 收束纪律

- 不自动执行 `openspec archive`。
- 不自动提交 git commit。
- 输出完成结论时必须说明验证结果、未覆盖风险和是否建议归档。
- Tier 2+ 以 `e2e-report.md` 的 `## 验收结论` 或用户确认的等价验收证据作为归档判断入口。
