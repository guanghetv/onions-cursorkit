# Backend Smoke Spec

## 来源

本文件是 smoke-test 外部 spec 样例，用于验证 `external-spec` 能把后端/API 类材料落到当前 change 中，并被后续验证阶段读取。

## 接口契约

无真实后端接口变化。

## 差异分析

- 与 `proposal.md` 一致：本变更只验证流程产物，不修改业务接口。
- 与 `specs/onion-sdd-flow-health/spec.md` 一致：恢复和归档判断依赖文档产物，不依赖后端运行时。

