# Tasks: fix-clarify-reask-ledger

> 脑暴写 prd；收敛后再 OpenSpec。禁止脑暴期强制写 proposal。

## 1. 收回错误阶段耦合

- [x] 1.1 `full-change`：脑暴记忆 = `prd.md`；禁止脑暴依赖 proposal
- [x] 1.2 `trellis-brainstorm`：No Re-Ask + Write-Before-Next-Ask 仅写 prd
- [x] 1.3 `openspec-change`：已确认决策表改为收敛后迁移说明
- [x] 1.4 `onsf-continue` / `onsf-plan`：分阶段读取 + 脑暴卡死防护

## 2. 同步副本

- [x] 2.1 `.agents` / `.claude` / `.cursor` 的 `trellis-brainstorm`
- [x] 2.2 `.cursor/skills/full-change` 与插件源对齐

## 3. 产物与验收

- [x] 3.1 更新 `proposal.md` / `spec.md` / 本 tasks / `prd.md` 与纠正后的阶段模型一致
- [x] 3.2 文档检索确认无「脑暴强制写 proposal」残留
- [x] 3.3 场景清单：答完不重问、压缩后不重问、Skip/Other
